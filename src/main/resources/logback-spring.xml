<?xml version="1.0" encoding="UTF-8"?>
<configuration>

	<springProperty scope="context" name="host" source="logging.gelf.host" defaultValue="localhost"/>
	<springProperty scope="context" name="port" source="logging.gelf.port" defaultValue="12201"/>
	<springProperty scope="context" name="environment-name" source="logging.environment.name"/>
	<springProperty scope="context" name="name" source="spring.application.name"/>

	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>
				%d{dd-MM-yyyy HH:mm:ss.SSS} %magenta([%thread]) %highlight(%-5level) %logger.%M - %msg%n
			</pattern>
		</encoder>
	</appender>
	<appender name="STDOUT-WARN" class="ch.qos.logback.core.ConsoleAppender">
		<filter class="ch.qos.logback.classic.filter.ThresholdFilter">
			<level>WARN</level>
		</filter>
		<encoder>
			<pattern>
				%d{dd-MM-yyyy HH:mm:ss.SSS} %magenta([%thread]) %highlight(%-5level) %logger.%M - %msg%n
			</pattern>
		</encoder>
	</appender>
	<appender name="SAVE-TO-FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${LOG_PATH}/${name}.log</file>
		<rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
			<fileNamePattern>${LOG_PATH}/${name}.%i.log</fileNamePattern>
			<minIndex>1</minIndex>
			<maxIndex>10</maxIndex>
		</rollingPolicy>

		<triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
			<maxFileSize>5MB</maxFileSize>
		</triggeringPolicy>
		<encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
			<Pattern>
				%d{dd-MM-yyyy HH:mm:ss.SSS} [%thread] %-5level %logger{36}.%M - %msg%n
			</Pattern>
		</encoder>
	</appender>
	<appender name="GELF" class="fbox.service.common.log.GelfUdpAppenderWithGZIP">
		<graylogHost>${host}</graylogHost>
		<graylogPort>${port}</graylogPort>
		<encoder class="de.siegmar.logbackgelf.GelfEncoder">
			<originHost>${environment-name}${name}</originHost>
		</encoder>
	</appender>

</configuration>